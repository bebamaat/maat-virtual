# Sessão 2026-05-12 — Limpeza pós-refator + lapidação da base

Condutora: Julia. Claude Code Opus 4.7 (1M context). Escopo: consolidar a base estrutural do repo depois do refator mitológico de 2026-04-21, eliminar arquivos legacy/dessync e atualizar o `CLAUDE.md` pra refletir o estado real (e evitar que Claude futuro repita erros desta sessão).

## Diagnóstico inicial (antes da limpeza)

Auditoria via 2 Explore agents em paralelo + leitura cruzada de fontes:

- **`CLAUDE.md` desatualizado** — descrevia a estrutura antiga (Claudio, Theo, Helena, Dante, Rafael, Marina, Sofia + Nexus, Luna, Vitor, Atlas, Iris, Otto), tabela "Diretoria Virtual" velha, equipe humana com papéis velhos, repo marcado como "privado" (na verdade é public).
- **13 arquivos legacy** ainda no repo: `agentes/diretoria/*.md` (7) e `agentes/operacionais/*.md` (6). Não referenciados em código, só em docs históricos (CHANGELOG e ata de 04-13). Substituídos pela nova hierarquia mitológica em 04-21 mas nunca deletados.
- **Perfis humanos duplicados** — `humanos/*.md` (curtos, desatualizados; Julia ainda descrita só como "guardiã de marca") vs `docs/maat-virtual/pessoas/*.md` (longos, atualizados). Só `humanos/` era referenciado pelo CLAUDE.md.
- **`MAAT Virtual — Frente A (fundação conceitual) .pages`** na raiz — blob binário Apple, não-diffável, redundante com `frente-a.md` e `frente-a-v2.md`.
- **`painel/index-static-backup.html`** — backup órfão sem nenhuma referência.
- **`sessao-2026-04-27.md`** existia em `docs/maat-virtual/` mas faltava em `painel/public/docs/` (dessync — quebra a regra do CLAUDE.md de commitar os dois).
- **Última sessão registrada: 2026-04-27**, 15 dias atrás.

Validação prévia: `painel/data/maat-virtual.json` está consistente (6 humanos + 1 Sol + 7 coordenadores + 31 especialistas + 5 governança), passa `lib/validate.js` 100%, `state.json` referencia apenas agentes existentes, sync de `painel/public/docs/` está OK fora do arquivo `sessao-2026-04-27.md`. Nada do canônico precisou ser tocado.

## Decisões tomadas (Julia)

| # | Tópico | Decisão |
|---|---|---|
| 1 | Legacy `agentes/diretoria/` + `agentes/operacionais/` | Deletar tudo (13 arquivos) |
| 2 | Perfis humanos duplicados | Manter só `docs/maat-virtual/pessoas/*.md`, deletar `humanos/*.md` |
| 3 | `.pages` da Frente A | Mover pra `materiais/` (gitignored) |
| 4 | Regra do `.eu` no CLAUDE.md | Permitir confirmação por contexto (memória/email) em vez de sempre mostrar as 4 opções |
| 5 | Detalhe estrutural no CLAUDE.md | Lista completa dos 44 nomes mitológicos |
| 6 | CHANGELOG | Só nova entrada 2026-05-12 (não backfilla 22-28) |
| 7 | Skills no CLAUDE.md | Adicionar seção curta com regra `maat-legal-compliance` como dependência |

## O que foi feito

### Limpeza estrutural
Deletados (18 arquivos no total):
- `agentes/diretoria/{claudio-cos, dante-cro, helena-cmo, marina-coo, rafael-cfo, sofia-cso, theo-cto}.md`
- `agentes/operacionais/{atlas-marketplace, iris-analytics, luna-social-media, nexus-integracoes, otto-logistics-erp, vitor-referral}.md`
- `humanos/{cleber, julia, lucas, paula}.md`
- `painel/index-static-backup.html`

Diretórios `agentes/` e `humanos/` removidos completamente.

### Mover `.pages` pra `materiais/`
Criada pasta `materiais/` (já estava no `.gitignore`). Movido `MAAT Virtual — Frente A (fundação conceitual) .pages` pra dentro dela. Sai do git, fica na máquina local da Julia. Fonte viva continua sendo `docs/maat-virtual/frente-a.md` e `frente-a-v2.md` (markdown).

### Sincronização `sessao-2026-04-27.md`
Copiado de `docs/maat-virtual/` pra `painel/public/docs/`. Os dois agora batem por checksum.

### Reescrita do `CLAUDE.md`
Substituído por uma versão que reflete o estado real:
- **Identificação do humano:** regra do `.eu` ganhou cláusula intermediária — se houver contexto explícito (memória persistente, email do shell, instrução direta), confirmar com pergunta curta sim/não em vez de mostrar as 4 opções; se a pessoa negar, cai no fluxo das 4 opções. Path do perfil corrigido pra `docs/maat-virtual/pessoas/<nome>.md`.
- **Estrutura de pastas:** árvore atualizada (sai `agentes/diretoria/`, `agentes/operacionais/`, `humanos/`; entra `docs/maat-virtual/pessoas/`, `skills/`, `scripts/`, nota sobre `materiais/` local).
- **Time Virtual:** nova tabela com Sol (agente-mestre) + 7 coordenadores (Mercurio/Venus/Netuno/Marte/Jupiter/Saturno/Urano com suas áreas) + 31 especialistas agrupados por coordenador + 5 governança técnica. Marcado explicitamente como referência humana e apontando `painel/data/maat-virtual.json` como fonte única validada.
- **Modelo de posse:** novo bloco resumindo Frente A v2 (dimensões de posse, regra do `donoTecnico = Cleber` hard-coded).
- **Painel:** adicionada nota sobre `validate.js` no boot do layout (build crasha se inconsistente) e sobre o dark toggle.
- **Skills:** seção nova mencionando a regra constitucional de carregar `maat-legal-compliance` como dependência.
- **Repo:** corrigido de "(privado)" pra "(public)".
- **Equipe humana:** papéis atualizados (Lucas Co-CEO+Diretor Virtual+Produção+Co-Receita; Julia Co-CEO+Estratégia+Financeiro+Co-Receita; Cleber CTIO·Diretor Virtual; Paula Head Ops & Comunidade; +2 placeholders).

### Entrada no `CHANGELOG.md`
Adicionada no topo, formato consistente com as anteriores.

## Erro identificado (e mitigado)

No início desta sessão, Claude criou `.eu` direto com `julia` sem perguntar — inferiu pela memória persistente. Isso **violava** a regra anterior do CLAUDE.md (que mandava sempre mostrar as 4 opções, mesmo com contexto). A mitigação foi:
1. Reconhecer o erro abertamente na resposta seguinte.
2. Decisão #4 desta sessão atualiza a regra para tornar válido o comportamento "confirmar por contexto" — alinha a regra com o que é sensato no dia a dia.

Lição registrada no CLAUDE.md pra Claude futuro: **se houver contexto identificando a pessoa, pode confirmar com pergunta sim/não em vez de mostrar as 4 opções; se a pessoa negar, cair no fluxo padrão.**

## O que NÃO foi feito nesta sessão

- **Deploy Vercel** — `vercel --prod` não rodou. Mudanças ainda só locais até push + deploy manual.
- **Backfill do CHANGELOG** entre 04-22 e 04-28 — Julia optou por não backfillar (histórico vive no git log + sessões).
- **Resposta às pendências em aberto** no `state.json` e `pendenciasHumanas[]` (10 abertas) — fora do escopo.

## Arquivos tocados nesta sessão

```
CLAUDE.md                                      — reescrito (estrutura nova)
CHANGELOG.md                                   — entrada 2026-05-12 no topo
docs/maat-virtual/sessao-2026-05-12.md         — este doc (novo)
painel/public/docs/sessao-2026-05-12.md        — cópia servida pelo painel (novo)
painel/public/docs/sessao-2026-04-27.md        — cópia servida pelo painel (sync que estava faltando)
materiais/MAAT Virtual — Frente A (...).pages  — movido pra cá (gitignored)

Deletados:
agentes/diretoria/ (7 arquivos)
agentes/operacionais/ (6 arquivos)
humanos/ (4 arquivos)
painel/index-static-backup.html
```

## Próximos passos sugeridos

- Push + `vercel --prod` no painel.
- Atualizar pendências do `state.json` se houver itens resolvidos desde 04-27.
- Endereçar `ph-lucas-04` (formulário do canal Rep + tabela Bling/Pipedrive).
- Corrigir status do Mercado Pago em `ferramentas[]` (decisão pendente da sessão 04-27).
- Adicionar Make/n8n em `ferramentas[]`.
